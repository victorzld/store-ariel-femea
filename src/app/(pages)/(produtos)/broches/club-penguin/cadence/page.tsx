import { Metadata } from "next";
import PinSix from "./cadence";

export const metadata: Metadata = {
    title: "Broche - Cadence",
};


export default function PagePinSix() {
    return (
        <PinSix />
    )
}